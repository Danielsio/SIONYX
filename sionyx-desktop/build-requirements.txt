# Build Requirements for SIONYX
# =============================
# Install these packages for the build process

# Core build tools
pyinstaller>=5.0.0

# Runtime deps required for packaging
PyQt6
PyQt6-WebEngine

# Firebase Storage upload (optional)
firebase-admin>=6.0.0

# Web build tools (if building web app separately)
# nodejs and npm should be installed separately

# Optional: For advanced packaging
# auto-py-to-exe  # GUI for PyInstaller
# cx-freeze       # Alternative to PyInstaller
# py2exe          # Windows-specific alternative

# Development tools
# pytest          # For testing
# black           # Code formatting
# flake8          # Linting
