<UserControl x:Class="SionyxKiosk.Views.Controls.PageHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:SionyxKiosk.Views.Controls">
    <UserControl.Resources>
        <local:StringToVisibilityConverter x:Key="StringToVisibility" />
    </UserControl.Resources>
    <Border CornerRadius="{StaticResource RadiusXl}" Padding="32,24"
            Background="{StaticResource HeroGradient}">
        <StackPanel>
            <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="{StaticResource FontSize2Xl}" FontWeight="Bold"
                       Foreground="White" />
            <TextBlock Text="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}}"
                       FontSize="{StaticResource FontSizeBase}" Foreground="#E0E7FF"
                       Margin="0,4,0,0"
                       Visibility="{Binding Subtitle, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource StringToVisibility}}" />
        </StackPanel>
    </Border>
</UserControl>
