<Window x:Class="SionyxKiosk.Views.Dialogs.PaymentDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        WindowStartupLocation="CenterOwner" Width="640" Height="720"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        FlowDirection="RightToLeft">
    <Border CornerRadius="16" Background="White" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="24" ShadowDepth="6" Direction="270" Color="#0F172A" Opacity="0.12" />
        </Border.Effect>
        <DockPanel>
            <!-- Header -->
            <Border DockPanel.Dock="Top" Padding="24,16" Background="{StaticResource HeroGradient}">
                <Grid>
                    <TextBlock Text="💳 תשלום" FontSize="18" FontWeight="Bold" Foreground="White" />
                    <Button Content="✕" Style="{StaticResource BtnGhost}" Foreground="White"
                            HorizontalAlignment="Left" VerticalAlignment="Center"
                            Click="CloseButton_Click" Width="36" Height="36" />
                </Grid>
            </Border>

            <!-- Package summary -->
            <Border DockPanel.Dock="Top" Padding="24,12" Background="#F8FAFC"
                    BorderBrush="{StaticResource BorderLightBrush}" BorderThickness="0,0,0,1">
                <Grid>
                    <TextBlock Text="{Binding Package.Name}" FontSize="16" FontWeight="SemiBold"
                               Foreground="{StaticResource TextPrimaryBrush}" />
                    <TextBlock HorizontalAlignment="Left" FontSize="20" FontWeight="Bold"
                               Foreground="{StaticResource PrimaryBrush}">
                        <Run Text="₪" /><Run Text="{Binding Package.Price, StringFormat='{}{0:F0}'}" />
                    </TextBlock>
                </Grid>
            </Border>

            <!-- WebView2 for payment form -->
            <wv2:WebView2 x:Name="PaymentWebView" />
        </DockPanel>
    </Border>
</Window>
