<Window x:Class="SionyxKiosk.Views.Dialogs.AdminExitDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Administrator Access" Width="440" Height="240"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" FlowDirection="LeftToRight"
        ShowInTaskbar="False">
    <Border CornerRadius="14" Background="White" Padding="32,28" ClipToBounds="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="48" ShadowDepth="6" Opacity="0.22" Color="#0F172A" />
        </Border.Effect>
        <StackPanel>
            <TextBlock Text="🔒  Administrator Access" FontSize="18" FontWeight="Bold"
                       FontFamily="Segoe UI" Foreground="#0F172A" Margin="0,0,0,6" />
            <TextBlock Text="Enter the admin password to exit the kiosk." FontSize="13"
                       FontFamily="Segoe UI" Foreground="#64748B" Margin="0,0,0,16" />
            <PasswordBox x:Name="PasswordInput" FontSize="14" FontFamily="Segoe UI"
                         Padding="12,10" Margin="0,0,0,24" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel" IsCancel="True"
                        Padding="24,10" Margin="0,0,10,0"
                        FontSize="13" FontWeight="SemiBold" FontFamily="Segoe UI"
                        Background="#F1F5F9" Foreground="#475569" BorderThickness="0"
                        Click="Cancel_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" Background="{TemplateBinding Background}"
                                    CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background" Value="#E2E8F0" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                <Button Content="Confirm" IsDefault="True"
                        Padding="24,10"
                        FontSize="13" FontWeight="SemiBold" FontFamily="Segoe UI"
                        Foreground="White" BorderThickness="0"
                        Click="OK_Click" Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="bg" CornerRadius="8" Padding="{TemplateBinding Padding}">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#6366F1" Offset="0" />
                                        <GradientStop Color="#4F46E5" Offset="1" />
                                    </LinearGradientBrush>
                                </Border.Background>
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="bg" Property="Background">
                                        <Setter.Value>
                                            <SolidColorBrush Color="#4338CA" />
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
