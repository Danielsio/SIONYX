<Window x:Class="SionyxKiosk.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SIONYX" Width="1280" Height="800"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="NoResize"
        FlowDirection="RightToLeft">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="260" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <Border Grid.Column="0" Background="{StaticResource SidebarGradient}">
            <DockPanel>
                <!-- Logo area -->
                <StackPanel DockPanel.Dock="Top" Margin="24,32,24,0">
                    <TextBlock Text="SIONYX" FontSize="28" FontWeight="ExtraBold"
                               Foreground="White" Margin="0,0,0,4" />
                    <TextBlock Text="{Binding CurrentUser.FullName}" FontSize="14"
                               Foreground="#FFFFFFBB" Margin="0,0,0,32" />
                </StackPanel>

                <!-- Logout button at bottom -->
                <Button DockPanel.Dock="Bottom" Content="התנתק" Style="{StaticResource BtnDanger}"
                        Command="{Binding LogoutCommand}" Margin="24,0,24,24" />

                <!-- Nav items -->
                <StackPanel Margin="12,0">
                    <Button Content="🏠  דף הבית" Style="{StaticResource BtnGhost}" HorizontalContentAlignment="Right"
                            Command="{Binding NavigateCommand}" CommandParameter="Home"
                            Foreground="White" Margin="0,4" Padding="16,12" />
                    <Button Content="📦  חבילות" Style="{StaticResource BtnGhost}" HorizontalContentAlignment="Right"
                            Command="{Binding NavigateCommand}" CommandParameter="Packages"
                            Foreground="White" Margin="0,4" Padding="16,12" />
                    <Button Content="📋  היסטוריה" Style="{StaticResource BtnGhost}" HorizontalContentAlignment="Right"
                            Command="{Binding NavigateCommand}" CommandParameter="History"
                            Foreground="White" Margin="0,4" Padding="16,12" />
                    <Button Content="❓  עזרה" Style="{StaticResource BtnGhost}" HorizontalContentAlignment="Right"
                            Command="{Binding NavigateCommand}" CommandParameter="Help"
                            Foreground="White" Margin="0,4" Padding="16,12" />
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- MAIN CONTENT -->
        <Border Grid.Column="1" Background="{StaticResource BgPrimaryBrush}">
            <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden" Margin="32" />
        </Border>
    </Grid>
</Window>
