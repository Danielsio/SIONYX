<Page x:Class="SionyxKiosk.Views.Pages.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="clr-namespace:SionyxKiosk.Views.Controls"
      Title="דף הבית" FlowDirection="RightToLeft">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="0">
        <StackPanel Margin="0,0,0,32">

            <!-- Page header with personalized greeting -->
            <controls:PageHeader Title="דף הבית" Subtitle="{Binding WelcomeMessage}" Margin="0,0,0,24" />

            <!-- Stats row -->
            <UniformGrid Rows="1" Margin="0,0,0,24">
                <controls:StatCard Title="זמן נותר" Value="{Binding RemainingTime}" Icon="⏱" Margin="0,0,12,0" />
                <controls:StatCard Title="יתרת הדפסות" Value="{Binding PrintBalance}" Icon="🖨" Margin="6,0" />
                <controls:StatCard Title="הודעות" Value="{Binding UnreadMessages}" Icon="💬" Margin="12,0,0,0" />
            </UniformGrid>

            <!-- Unread messages card (visible only when there are unread messages) -->
            <Border x:Name="MessageCard" Background="#EFF6FF" CornerRadius="{StaticResource RadiusLg}"
                    Padding="20,16" Margin="0,0,0,16" Cursor="Hand"
                    MouseLeftButtonUp="MessageCard_Click"
                    Visibility="Collapsed">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="💬" FontSize="24" VerticalAlignment="Center" Margin="0,0,12,0" />
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="הודעות חדשות" FontSize="15" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryBrush}" />
                        <TextBlock x:Name="MessageCountText" Text="" FontSize="13"
                                   Foreground="{StaticResource TextSecondaryBrush}" />
                    </StackPanel>
                    <TextBlock Grid.Column="2" Text="צפה בהודעות ←" FontSize="13" FontWeight="SemiBold"
                               Foreground="{StaticResource PrimaryBrush}" VerticalAlignment="Center" />
                </Grid>
            </Border>

            <!-- Session controls -->
            <Border Background="White" CornerRadius="{StaticResource RadiusLg}" Padding="32,24" Margin="0,0,0,24">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="12" ShadowDepth="3" Direction="270" Color="#0F172A" Opacity="0.06" />
                </Border.Effect>
                <StackPanel>
                    <TextBlock Text="ניהול הפעלה" Style="{StaticResource TextH3}" Margin="0,0,0,16" />

                    <!-- Session inactive: show Start button -->
                    <StackPanel HorizontalAlignment="Center"
                                Visibility="{Binding IsSessionInactive, Converter={StaticResource BoolToVis}}">
                        <Button Content="▶  התחל הפעלה" Style="{StaticResource BtnPrimary}"
                                Command="{Binding StartSessionCommand}"
                                Width="220" Height="52" FontSize="17"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBool}}" />
                    </StackPanel>

                    <!-- Session active: show Resume + End buttons -->
                    <StackPanel HorizontalAlignment="Center"
                                Visibility="{Binding IsSessionActive, Converter={StaticResource BoolToVis}}">
                        <TextBlock Text="ההפעלה פעילה" FontSize="14" FontWeight="SemiBold"
                                   Foreground="{StaticResource SuccessBrush}"
                                   HorizontalAlignment="Center" Margin="0,0,0,12" />
                        <Button Content="▶  חזור להפעלה" Style="{StaticResource BtnPrimary}"
                                Click="ResumeSession_Click"
                                Width="220" Height="48" FontSize="16" Margin="0,0,0,10" />
                        <Button Content="⏹  סיים הפעלה" Style="{StaticResource BtnDanger}"
                                Command="{Binding EndSessionCommand}"
                                Width="220" Height="48" FontSize="16" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Loading indicator -->
            <controls:LoadingSpinner HorizontalAlignment="Center"
                                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

            <!-- Error / warning banner -->
            <Border Background="#FEF2F2" CornerRadius="12" Padding="20,16" Margin="0,8,0,0"
                    Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVis}}">
                <Border.BorderBrush>
                    <SolidColorBrush Color="#FECACA" />
                </Border.BorderBrush>
                <Border.BorderThickness>1</Border.BorderThickness>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⚠" FontSize="20" VerticalAlignment="Center" Margin="0,0,12,0" />
                    <TextBlock Text="{Binding ErrorMessage}" Foreground="#991B1B"
                               FontSize="15" FontWeight="SemiBold" TextWrapping="Wrap"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
